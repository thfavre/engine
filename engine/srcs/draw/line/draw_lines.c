#include "vector2.h"
#include "image.h"

// void	engine_draw_lines(t_img *img, t_vector2 **points, int color, int thickness)
// {
// 	int	i;

// 	if (!points)
// 		return ;
// 	i = 0;
// 	while (points[i] != NULL && points[i + 1]  != NULL )
// 	{
// 		engine_draw_line(img, points[i], points[i + 1], color, thickness);
// 		i++;
// 	}
// 	engine_draw_line(img, points[i], points[0], color, thickness);
// }
